[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip3 install fastapi uvicorn jinja2 python-dotenv groq httpx --break-system-packages"
]

[start]
cmd = "python3 -m uvicorn dashboard.app:app --host 0.0.0.0 --port $PORT"
