[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "python3.11 -m pip install fastapi uvicorn jinja2 python-dotenv groq httpx"
]

[start]
cmd = "uvicorn dashboard.app:app --host 0.0.0.0 --port $PORT"
